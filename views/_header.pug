header.header
    nav.nav.nav--tours
        .header__logo
            img(src='/img/holidays.png' alt='Holidays logo')
        a.nav__el(href='/') Holidays

    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout(href='#') Log out
            a.nav__el(href='#')
                img.nav__user-img(src=`/img/users/${user.photo[0]}` alt='User photo')
                span Jay
        else
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el--cta(href='/signup') Sign up